FROM shib_base

USER shibd
EXPOSE 1600

COPY shibboleth2.xml.shibd /etc/shibboleth/shibboleth2.xml
 
# TODO this needs to change to a mount point to work with cron
COPY get-sealer-keys/keys /etc/shibboleth/

ENV LD_LIBRARY_PATH /opt/shibboleth/lib64

ENTRYPOINT [ "/usr/sbin/shibd", "-F", "-f" ]
